# Runtime dependencies for greek_squeezes.ipynb
# Developed on Python 3.12; Python 3.10–3.12 recommended.

# --- Deep learning / models ---
torch>=2.2                # CPU/MPS wheel on macOS; CUDA wheel on Linux. See README for GPU notes.
transformers>=4.41        # TrOCR + DINOv2
accelerate>=0.30          # required by transformers' Seq2SeqTrainer (fine-tuning)
timm>=0.9                 # vision backbones used by some transformers models
einops>=0.7               # tensor ops used by some transformers models

# --- Vision / imaging ---
opencv-python>=4.8        # cv2: preprocessing (illumination flatten, NLM denoise, CLAHE)
pillow>=10.0              # PIL: image I/O

# --- Scientific stack ---
numpy                     # left unpinned so the resolver matches numba/umap constraints
pandas>=2.0
scikit-learn>=1.3         # PCA, TruncatedSVD, KMeans, silhouette, ridge scorer
matplotlib>=3.7

# --- Embedding viz + OCR metric ---
umap-learn>=0.5           # UMAP projection of DINOv2 embeddings
textdistance>=4.5         # Levenshtein, used by the official TROGS CER evaluation

# --- HTTP (LLM proxy calls + dataset fallback download) ---
requests>=2.31

# --- Run the notebook locally ---
jupyterlab>=4.0
ipykernel>=6.29
